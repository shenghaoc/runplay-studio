# Design: Recorded Lap Preservation

## Terminology

- **Calculated split** (`RunSplit`): regular distance interval generated by the app.
- **Recorded lap** (`RecordedLap`): boundary and optional summary from the source file.
- **Route segment**: continuous geometry; independent of laps and of movement stops.

## Models

- `RecordedLap` — Identifiable/Codable/Hashable/Sendable with route-derived clocks and paces.
- `RecordedLapTrigger` — manual, distance, time, position, sessionEnd, fitnessEquipment, unknownFIT, unknownTCX, unavailable.
- `RecordedLapReportedMetrics` — source elapsed/timer/distance/ascent/HR/cadence/speed/calories/raw trigger.
- `RecordedLapDiagnostics` — aggregated import/analysis counts and reimport flag.
- `RunWorkout.recordedLaps`, `sourceStructureVersion`, `recordedLapDiagnostics`.
- Analysis version bumped to 5.

## Timeline

`WorkoutTimeline` adds `elapsedSample(at:boundary:)`, `timeRange(from:to:)`, and
`distance(atElapsedTime:boundary:)` with half-open ownership and no geographic
interpolation across recording gaps.

## Analysis

`RecordedLapAnalyzer` maps source timestamps onto elapsed bounds, uses shared
timeline/movement/elevation context, enforces clock invariants, and aggregates
mismatch diagnostics. Importers attach provisional source laps; analysis fills
canonical fields while preserving IDs.

## Import

- **FIT**: filter laps to selected session; map triggers and enhanced speed fields; never create segments.
- **TCX**: parse lap metadata; continuity via `TCXRouteContinuityResolver`; per-track distance rebasing only.
- **GPX**: empty recorded laps; current structure version.
- **JSON**: optional recorded laps round-trip.

## Presentation

Splits tab gains Distance Splits / Recorded Laps modes. Replay exposes
`SelectedMetrics.recordedLapIndex`. Comparison adds ordinal lap pairing.
Exports add Recorded Laps CSV, combined section, and JSON lap fields.

## Legacy policy

Old FIT/TCX snapshots decode empty laps with structure version 0. Non-fatal
reimport note; no fabricated data; app launch never blocked.
