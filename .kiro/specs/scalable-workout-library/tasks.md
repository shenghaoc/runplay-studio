# Tasks: Scalable Workout Library

- [x] Manifest v2 + migration + favourite recovery
- [x] Lightweight entries, search documents, query models, query service
- [x] Metadata editing policy + actor `setFavorite` / `updateWorkoutMetadata`
- [x] `WorkoutLibraryViewModel` with cancellation and stale suppression
- [x] All Runs UI table + metadata editor sheet
- [x] Bounded sidebar + workspace integration in AppState/ContentView
- [x] Core + Studio tests for query, manifest, actor, navigation, view model
- [x] Documentation and Kiro steering updates
- [x] Full suite + packaged app + CI green on final head
