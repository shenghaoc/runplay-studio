# Tasks — C++23 Route-Metric Scale and Buckets

- [x] Verify live `origin/main`, PR #94 exact-head CI, open PR overlap, branch,
  worktree, and inherited baselines.
- [x] Capture three release product-limit metric-profile baselines outside the
  repository.
- [ ] Characterize current Swift custom-policy and numeric edge behavior.
- [ ] Add the allocation-free public C++23 bulk boundary and implementation.
- [ ] Add native boundary, policy, statistic, normalization, bucket, and large
  fixture coverage under normal, ASan, and UBSan runners.
- [ ] Add the pure-Swift Interop result surface and strict native validation.
- [ ] Add an independent Swift oracle and at least 1,500 deterministic bridge
  parity fixtures.
- [ ] Cut `RouteMetricProfileBuilder.finalizeProfile` over to exactly one native
  call while retaining Swift extraction, smoothing, labels, and public models.
- [ ] Add complete profile, Platform, Studio/cache, export, and cancellation
  parity coverage.
- [ ] Extend AST and boundary validation without weakening existing checks.
- [ ] Add the opt-in release benchmark and pass performance/memory gates.
- [ ] Update durable architecture/roadmap documentation.
- [ ] Run the complete verification matrix and exact-head CI.
