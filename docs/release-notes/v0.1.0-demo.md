# RunPlay Studio v0.1.0-demo

First public demo release of RunPlay Studio — a native macOS post-run 3D route
replay and analysis app.

## Headline Features

- **3D route replay** — SceneKit-rendered 3D route with start/finish markers,
  kilometer markers, position cone, and adaptive ground grid
- **Route coloring** — Pace-based (blue=fast, red=slow) and elevation-based
  (green=low, brown=high) coloring with quantile normalization
- **Synchronized replay** — 3D, map, charts, metrics, and split table all stay
  in sync with the timeline; chart click/drag-to-seek
- **Segment detection** — Automatic identification of fastest 400m, fastest 1km,
  slowest 1km, biggest climb, and biggest descent
- **Route comparison** — Side-by-side comparison of two runs with summary deltas,
  per-split table, pace chart, 2D overlay, and 3D overlay
- **3D comparison distance slider** — Scrub along common route distance with
  interpolated markers and elapsed time/pace delta readout
- **Export** — Local JSON summary, splits CSV, segments CSV, combined CSV, and
  PNG summary card (1200×1600)
- **Local-only privacy** — No account, no cloud, no telemetry, no analytics, no
  AI API; all workout data stays on your Mac

## Supported Import Formats

| Format | Status |
|--------|--------|
| JSON   | Full support (native format) |
| GPX    | Track support with HR/cadence extensions |
| TCX    | Full support with laps, HR, cadence, distance |
| FIT    | Basic support (GPS, altitude, HR, cadence) |

## Supported Export Formats

- JSON summary
- Splits CSV
- Segments CSV
- Combined CSV
- PNG summary card

## Privacy Statement

RunPlay Studio is a local-only application. No workout data leaves your Mac.
The only network activity is Apple MapKit loading map tiles for the map view.
No cloud sync, no accounts, no telemetry, no analytics, no AI APIs.

## Known Limitations

- Comparison is distance-aligned only — no dynamic time warping or route matching
- FIT support is basic (no compressed timestamps, no CRC validation)
- No HealthKit integration
- No cloud sync, accounts, or web interface
- macOS only (requires SwiftUI, SceneKit, MapKit)
- PNG export requires GUI context (NSHostingView)

## Test Status

- **433 tests**, 0 failures
- **114 core tests** (platform-neutral, no UI framework dependencies)
- CI: GitHub Actions on macOS and Linux (Core)

## Manual QA Status

All GUI checklists verified by human owner on 2026-07-08:

- ✅ 3D camera controls (Fit Route, presets, orbit/zoom)
- ✅ 3D comparison view
- ✅ Selected-distance slider
- ✅ Comparison chart readability (legend, units, empty states)
- ✅ Save-panel JSON, CSV, and PNG export
- ✅ Default view (Overview tab, map, metrics, replay controls)
- ✅ Delete UI (context menu, confirmation, deletion, empty state)
- ✅ HR color mode (disabled state, tooltip, normal operation)

## Build Requirements

- macOS 14.0+
- Xcode 15.0+ or Swift 5.9+ toolchain

## Quick Start

```bash
git clone https://github.com/shenghaoc/runplay-studio.git
cd runplay-studio
open Package.swift
```

Select the **RunPlayStudio** scheme, choose **My Mac**, press **⌘R**.

See [docs/demo-script.md](../demo-script.md) for a 3–5 minute walkthrough.
